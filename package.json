{
  "name": "expense-tracker-parent",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "Backend",
    "Client"
  ],
  "scripts": {
    "install-all": "npm install && npm install --prefix Backend && npm install --prefix Client",
    "build-client": "cd Client && npm install && npm run build",
    "build-backend": "cd Backend && npm install",
    "dev": "concurrently \"npm run start --prefix Backend\" \"npm run start --prefix Client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}